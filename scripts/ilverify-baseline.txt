# ilverify baseline allowlist for N#
#
# Each non-comment line is a KNOWN/EXPECTED IL verification finding,
# normalized as:  <Assembly.dll> | <kind> | <detail>
#   kind = IL:<Code>  ECMA-335 verification error (e.g. IL:StackUnexpected)
#   kind = MD          metadata error (e.g. missing interface method)
#   kind = CRASH       ilverify itself crashed importing this assembly
#
# The gate (scripts/ilverify.sh) fails only on findings NOT listed here,
# so it blocks NEW unverifiable IL (the PR #160 regression class).
# Keep this file as close to EMPTY as possible: every entry is real debt.
# Regenerate with: scripts/ilverify.sh --update-baseline (review the diff!).
#
# Generated 2026-05-30T16:06:42Z
#
