{
  "schemaVersion": 1,
  "case": "JSON metadata CLI path",
  "expected": {
    "boundary": "EmitJson",
    "hot": "Score",
    "aot": "JsonSerializer metadata is required",
    "nativeImageEmitted": false
  }
}
