name: NSharpLang.CensusExternalLookup.Tests
version: 1.0.0
backend: il
outputType: library
targetFramework: net10.0

dependencies:
  - project: ../../fixtures/census-external-lookup-library/project.yml
