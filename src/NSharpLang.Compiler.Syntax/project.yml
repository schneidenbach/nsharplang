name: NSharpLang.Compiler.Syntax
backend: il
outputType: library
targetFramework: net10.0
# Syntax carries its own estate (`*.tests.nl`); every build of it that does not pass
# `-p:NSharpExcludeTests=false` is product-only, which is what the slices above it reference.
excludeTests: true
dependencies:
  - project: ../NSharpLang.Compiler.Model/project.yml
