namespace NSharpLang.Playground.Wasm;

public static class Program
{
    public static void Main()
    {
        // JS callers use PlaygroundExports. Main only satisfies the browser-wasm app entry point.
    }
}
