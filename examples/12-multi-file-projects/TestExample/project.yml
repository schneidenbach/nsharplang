name: TestExample
version: 1.0.0
outputType: library
targetFramework: net10.0

# All .nl files are automatically discovered (excluding .tests.nl files)
# To exclude additional files, use the exclude list:
# exclude:
#   - temp/**/*.nl
#   - old-code.nl

language:
  asyncDefaultType: ValueTask
