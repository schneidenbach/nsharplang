{
  "name": "issue-tracker",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && ../scripts/nlc-local.sh restore && dotnet watch --project IssueTracker.csproj run --urls http://localhost:5167",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "npm run build:frontend && npm run build:backend",
    "build:backend": "cd backend && ../scripts/nlc-local.sh restore && dotnet build IssueTracker.csproj",
    "build:frontend": "cd frontend && npm run build",
    "test:backend": "cd backend && ../scripts/nlc-local.sh test",
    "demo": "./scripts/demo.sh",
    "install:frontend": "cd frontend && npm install",
    "setup": "npm install && npm run install:frontend"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
