# S2.2(j): accepted iterator realization

Product `20caf6997c00757496c146fb143dc4ca11b76f2d` is the exact integration of Sol
`72278d7174aa710dd03e2fc8ca80b1b0b5e75a14`. Frozen native controls are `eaa10aaf5`, ratchet is
`269ead8db49ed5399051eaa7a68118bf9fba6882`, and final persisted controls/tested source are
`4b5399140ffd9babff214790af63e036bd1fbb49` (Terra `d1ea440b50388a2ab0883af4472673e878debf8e`).
Accepted predecessor is `c5cf65144c6951f58454c32de703f0b2e1bd5cb6`; its j1 seed remains installed.
The immutable proof directory is `/private/tmp/nsharp-023-s22j-proof-20260906`.

## Ownership and source

Both iterator realization drivers and their exclusive canonical resolver/recursive guard moved to
N#. C# retains exact 16/7-argument forwards and existing ambient decline recording with default spans
−1/0. All 11 interleaved member attachment/body phases, both raw constructors and factory-before-append
order remain. Exact dictionary equality, short-circuit/out/null behavior and source T[] refusal remain.
The strict Type cast compiles to the same raw element-type dereference, with no added null check.

The emitter is 18,974 lines /18,035 nonblank /988,649 bytes, a reduction of 290/265/18,756.
Its SHA256 is `67c1f88184af56381d69799b1c38910ea4a2c6496c68fd8f2db8f41740b454bd`.
The original 15 iterator AddType sites now select the exact runtime companion on the caller's table;
three production contexts retain that table lazily. Global census is 36 sites /12 files /21 keyed /
15 handle-only. Member discovery, raw constructor planning, remaining type consumers and later writer
stages remain open; this does not claim terminal emitter or branch completion.

## Executed evidence

Sol's forced test-enabled estate passes 7,801/7,801, with three new contracts for all 15 consumed rows,
heterogeneous captured field type, conditional/no-box/no-capture/null-table behavior, explicit
TaskAwaiter before bake, and rejection of a fresh corrupted runtime companion before IL/local mutation.
Terra's two additional direct controls pass 2/2 on exact final source. They use the actual persisted
TypeBuilderImpl field inventory to retain the original FieldBuilder, rebind it under a factory-MVAR
owner and validate the observed machine-VAR field key/companion. The other control observes retained sync
registration when element resolution declines and async absence under the actual ordinal 18 key with unchanged
registry count. This is pre-bake phase/handle evidence; native suites establish runtime execution.
The fixture's retained-field inventory is consistent with the
[.NET10 TypeBuilderImpl source](https://github.com/dotnet/runtime/blob/v10.0.0/src/libraries/System.Reflection.Emit/src/System/Reflection/Emit/TypeBuilderImpl.cs).

Fixed f543 corpus: 75 targets, 73 successes, 94 normalized PE images and 2,184 native passes per arm.
There are zero image, path-set or normalized-outcome differences; the same two NL402 template declines
remain. The harness's `IL_DIFFS` field denotes normalized whole-PE differences, not a separately
disassembled text-IL comparison. The frozen broad-j native source passes 109/109 under the immutable
candidate, and its normalized test image equals the still-verified original i baseline artifact.
Strict JSON is byte-identical pre/post over the same final source: 258 ordered findings across 434 files,
with no added, removed or shifted diagnostics. The extra checked file is the new N# owner.

Fresh exclusive `VSCODE_TESTS=skip ./scripts/test-all.sh --commit` at the exact tested revision passed
in 458s: 593 unit, 7,803 canonical, 109 native declarations, 7 Reflection.Emit bootstrap, 15 records,
18 ownership tests, 52 native projects and 68 IL-verified assemblies. Benchmark correctness passed;
front-end timing was unjudged because of host load. This backend-only gate does not replace the final goal's IDE verification.
All 12 accepted j1 live SDK payload entries remain byte-identical; no seed repin was needed. Legacy
validation remains bootstrap debt.

The 381-row ratchet changes only the emitter current metrics/fingerprint and both head keys; no epoch
or unrelated ceiling changes. Audit is 17/18 before repin and 18/18 afterward. Head is
`head-v1:10ec853d6982a651`; emitter text is `text-v1:8ee10f925c3454bb`.

## Retained receipts and limits

- `/private/tmp/gate-20260906-goal-s22j-r1/gate-result.json` links the fresh gate and diagnostics.
  `acceptance.json` adds the reviewed Markdown-only follow-up and exact verified remote revision.
- Proof directory `compiler-pre.json`/`compiler-post.json`, `compare-j1-j.json`, `coverage.json`,
  `strict/strict-comparison.json`, `native-candidate/receipt.json`, `census-after.json`, and both
  audit receipts pin commands, source, output and immutable artifacts.
- Sol handoff: `/private/tmp/nsharp-s22j-realization-executor-logs/final/receipt.json`.
  Terra final: `/private/tmp/nsharp-s22j-realization-controls-logs/direct-19-final-two-exact/receipt.json`.
- Independent reviews: `/private/tmp/nsharp-s22j-review/realization-final-compiled/review.json`,
  `immutable-post/review.json`, `persisted-final-controls/review.json` and
  `acceptance-evidence/review.json`. Immutable 14 CLI/77 support payloads and actual forwarder/owner IL
  are linked separately from runtime test evidence.

Earlier dev invocations excluded N# tests and do not prove the new assertions ran. Failed test
spellings, post-bake/unconditional-fixture mistakes and the initial wrong async key were corrected;
zero-selection runs are not counted. Source-unfrozen intermediate logs are explicitly limited to
iteration history. The final source, commands, TRX and artifacts are retained. Extra direct-GetType
and baked generic GetField probes failed at their named source/BCL boundaries and are not acceptance
claims or product changes. No assertion was removed from an existing accepted product contract.

The next concrete slice is [S2.2(k), member iterator discovery](2026-09-06-s22k-member-iterator-next-cut.md).
Tasks 015/021/022/023 remain unchecked until their full terminal conditions pass.
