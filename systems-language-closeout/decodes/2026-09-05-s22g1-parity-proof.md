# S2.2(g1): immutable integration proof

Product `f243d421c1a0848a8dc7f47265a9275eac56a862` cherry-picks worker `765776ce` over accepted
`f54385d5d6b32efb0cb47e5761931bb63af707f4`. This four-line N# field-plan prerequisite does not move
source discovery ownership. The integration documentation is the only subsequent tree change.

## Immutable inputs and scope

Evidence root: `/private/tmp/nsharp-023-s22g-seeded-proof-20260905/`.
`compiler-pre.json` pins a fresh actual f543 compiler; `compiler-prerequisite.json` pins f243.
Each retains all 14 CLI and 77 support payloads. The prerequisite archive SHA256 is
`7ac8a693674d9cf2ff4963b7eeb4477e0635b726aa7b4e0dc9ca20db73c29232`.
The fixed corpus is the accepted f543 archive
`2d859a90e8762e3edb7d917c715477d805566560cd7daf51f261e11bcf09a257`.

Arms d/e/p each cover 75 targets, 73 successful projects, 94 emitted images and 2,184 passed native
tests, zero failed/skipped; two systems-template NL402 refusals are unchanged. `compare-d-e.json` and
`compare-d-p.json` report zero whole normalized image, output-set or full-outcome differences.
`prerequisite-coverage.json` records counts independently. The fixed declaration suite contains104
tests; `native-discovery-baseline/receipt.json` verifies55 physical MethodImpl rows and the predeclared
literal discovery/slot controls. The additional g1 field fixture is not silently added to this corpus.

`nonvacuity.json` detects the expected one-byte constant/output change. The freshly generated closed
ISourceSlots<int> attachment fixture in `methodimpl-control/` detects the predicted First41→42 change
from a one-byte MethodImpl.MethodBody mutation while Second stays42. This proves attachment sensitivity;
it does not substitute for pending discovery exception/out-timing mutation coverage.

## New capability, strict analysis and review

`g1-capability/verdict.json` replays identical final source
`ed47ded9bdce93468918d3679a561ca0114fe9b92cf69936e68508cf1a486cde`: actual f543 declines at the short
field read before tests, while f243 passes1/1 with empty stderr. Four actual BCL ldsfld sites and typed
out stind.ref are recorded. The oracle compares reference identity against BCL Array.Empty<Type>()
via test-only reflection. Initial unsupported array-cast oracle attempts are retained by the worker;
the passing oracle holds object and compares directly.

Worker final evidence `/private/tmp/nsharp-s22g-controls-logs/type-empty-types/final/receipt.json`
records forced canonical estate7,759/7,759 and full native declarations105/105. Its corrected manifest
pins the actual generating CLI-sibling BootstrapServices payload; the initial estate-payload
misattribution remains recorded. The worker's frozen pre was70, not f543; the implementation decode
now distinguishes that attempt from root's actual f543 same-source proof.

`strict-prerequisite/strict-comparison.json`:432 checked files,259 errors, zero warnings/info; identical
final-source pre/prerequisite JSON bytes and unchanged-line mapping for every inherited finding, no
added/removed diagnostic. `root-format-receipt.json` pins all four N# file hashes and zero format drift.
`baseline-census.json` and `prerequisite-census.json` agree: emitter19,484 lines/18,505 nonblank/
1,014,658 bytes; AddType36 calls/12 files/1 keyed/35 handle-only; no C# change and no ratchet movement.

Independent immutable source/payload/consumed-IL review passes at
`/private/tmp/nsharp-s22g-review/g1/immutable-prerequisite/README.md`
(SHA256 `e5d6f9815cd6b3267294a952febf02700dc160309c4b4ff3c68fca1eb604873d`).
`linkage.json` verifies all1,792 tracked archive files,14CLI/77support hashes, unchanged existing planner,
exact field row and field identity checks, four native reads and all five oracle/helper bodies.

## Checkpoint and remaining work

The integration commit requires a fresh exclusive
`VSCODE_TESTS=skip ./scripts/test-all.sh --commit` run, followed by
`./scripts/setup-local.sh --skip-vscode --no-path-update` and a real SDK-built Type.EmptyTypes fixture.
These steps are pending at this documentation commit. The source archive, command/exit/log, seed cache
and package linkage, actual SDK task path, probe results, push and acceptance are retained under
`/private/tmp/gate-20260905-goal-s22g1-r1/`. Read actual acceptance receipts before resuming consumers.
The isolated gate does not refresh the live SDK. A stale same-version SDK cache was measured on g0;
compare packaged tools with the cache before using it and preserve any refresh evidence.

The next discovery cut must delay registry.Values until the original direct/closed guards/head reads,
preserve caller versus local out writes and first-hit disposal timing, and use the real BCL empty array.
A try/yield/finally-throw fixture declines with `emit.iterator.unsupported-shape`; the replacement
N# test-only throwing enumerator is drafted but unexecuted. No disposal-throw coverage or C# discovery
deletion is claimed here. No IDE behavior changes. All terminal task015/021/022/023 boxes remain open.
