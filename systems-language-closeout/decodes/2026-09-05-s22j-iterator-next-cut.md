# S2.2(j): iterator realization and consumed structural types

Source pin: `2d156b43b282807f9ce0909b711f9c88da584ff6`, reviewed while i awaited checkpoint acceptance. Nine of ten original pinned files are unchanged; only the emitter changed for record ownership. Its entire iterator helper region remains byte-identical to accepted b1. Exact source hashes and calls are in `/private/tmp/nsharp-s22j-assessment/revalidation-2d156b43b.json`. That initial assessment started no implementation. Subsequent seed probes found the j0 Ldftn prerequisite and the existing source T[] refusal, recorded below; broad j remains unimplemented.

**Connected deletion.** Move `TryEmitIteratorStateMachine` (2503–2681), `TryEmitAsyncIteratorStateMachine` (2686–2789), and their exclusive `TryResolveIteratorCanonical`/`ContainsMethodVarReference` closure (2868–2905) into an N# realization owner beside the existing body planner. The four declarations total **318 C# lines before replacement wrappers**; calculate actual net shrink from implementation. Corrected endpoints include the two closing braces omitted from the earlier rough ranges. Retain old-signature wrappers only to forward inputs and immediately record returned decline facts through existing `DeclineStatic`. No declaration/field/member/factory loops, rebinding decisions, constructor IL or canonical admission remain in them. `TryEmitMemberIterator` (2794–2862) stays separate enclosing-member discovery/admission, supplying the owner at 2810/2858; top-level calls are 4168/4170. No hidden C# callback or widened async/generic instance admission.

**Consumed identity.** The unchanged planner has exactly **15 raw AddType calls**: nine state-machine entries and bool, Exception, ValueTask, conditional boxed element, captured factory-field type and TaskAwaiter. Pass the existing catalog table through `ColumnarIteratorEmitContext` (1202/1225) and its already-retained recursive MoveNext context. Select exact runtime companions at each old AddType evaluation; no eager selection, fresh-table fallback or signature matrix. If no new calls appear, the projection is 6→21 keyed and 30→15 legacy out of 36. Method/field pool identities are outside this claim.

Machine/synthesized VAR registration already occurs at 2545/2708. Factory method MVAR registration is now `ForSourceMethod` call 4009, registry 1225. Generic members use machine self-instantiation; factories separately rebind over method MVARs. Before broad edits, execute a real generator with bare T captures under int and string instantiations and assert runtime enumeration. Preserve the real parsed/emitted T[] capture refusal: `emit.iterator.field-type`, message `iterator hoisted field type 'T[]' could not be resolved for 'IteratorRealizationUnsupportedTArray'`, member `IteratorRealizationUnsupportedTArray`. The canonical exact-resolver route constructs a method-MVAR array before the machine-map fallback; the existing recursive MVAR guard rejects it. Do not change resolver precedence or widen source admission. Separately labeled internal factory/table controls may use actual MakeArrayType and rebound TypeBuilder.GetField(...).FieldType handles for T/T[] and assert exact self/factory/field key companions; they do not demonstrate an admitted T[] source program. Consume actual rebound `FieldInfo.FieldType` at factory 1775, never a reconstructed signature hiding VAR/MVAR differences. An invalid/unregistered exact companion is a named prerequisite. Preserve the existing sync generic factory elementType and omitted enclosing/known extras.

**Order.** Sync defines/registers before element resolution; async resolves/admit before defining/registering. Preserve sync hoisted-enumerator special resolution, direct machine-map priority, false/out values and recursive MVAR checks. Replay both raw constructors in N# at their existing phase, including async continuation/core handles; typed constructor CodePlan conversion is not claimed. Preserve all 11 Apply→BuildPlan→GetILGenerator→Execute phases, async core-before-interface order, and factory completion before synthesizedTypes.Add. Return decline facts at the old failure points; the wrapper records unchanged site/message/member/span under the existing source-file scope. No callback or copied diagnostic storage owner.

**Acceptance and stop condition.** Reuse native sync/generic/async suites and the precomputed-shape ordering seam, positive twins and earlier body/attachment progress on later failures. Add actual 15-site keyed/table assertions plus fresh-plan companion corruption before IL/local creation; never reuse a consumed positive. Cover self/factory owners, generic Current boxing, hoisted disposal, enclosing this, async completion/exception/await and decline timing. First measure exact rebinding, raw constructor Emit and table selection under the live seed. Any failure needs a bounded prerequisite before this full cut; no partial driver/raw-row fallback/admission widening qualifies. **Prerequisite accepted:** i is accepted at `9f8eaf6c4`. j0 product `74e0c908a`, wording `9be5346d4` and native/tested `b57676617` pass the fresh backend gate and installed SDK 1/1 probe recorded in [j0 proof](2026-09-06-s22j0-parity-proof.md). First run the direct declaration/generic-rebound stage-zero probes under that accepted SDK; the nongeneric native continuation fixture does not establish generic field identity or the full direct declaration surface.

**Frozen eligible j controls, not integrated:** Terra `86405783b42affbb7e66b841912eb185e18b57bf` adds `IteratorRealizationEmitFacts.tests.nl` (SHA256 `a7fe0f86e2ffd6cfc545b5063a44b5ce23c8d7aa5a7360ee9434d1ad06748bfd`). Immutable i baseline 109/109 is recorded at `/private/tmp/nsharp-s22j-controls-logs/native-realization/run-08/receipt.json`, SHA256 `ff5de18172ccc4f94ed186e665546f8ac9f8b7002ad4a4d87d319e4ccb760b77`. Integrate it with the completed j owner; do not count it in j0's 107 native declaration tests.
