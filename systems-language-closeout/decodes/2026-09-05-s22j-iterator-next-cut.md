# Contingent S2.2(j): iterator realization and consumed structural types

Source pin: `2d156b43b282807f9ce0909b711f9c88da584ff6`, reviewed while i awaited checkpoint acceptance. Nine of ten original pinned files are unchanged; only the emitter changed for record ownership. Its entire iterator helper region remains byte-identical to accepted b1. Exact source hashes and calls are in `/private/tmp/nsharp-s22j-assessment/revalidation-2d156b43b.json`. No implementation, builds or probes were started.

**Connected deletion.** Move `TryEmitIteratorStateMachine` (2503–2681), `TryEmitAsyncIteratorStateMachine` (2686–2789), and their exclusive `TryResolveIteratorCanonical`/`ContainsMethodVarReference` closure (2868–2905) into an N# realization owner beside the existing body planner. The four declarations total **318 C# lines before replacement wrappers**; calculate actual net shrink from implementation. Corrected endpoints include the two closing braces omitted from the earlier rough ranges. Retain old-signature wrappers only to forward inputs and immediately record returned decline facts through existing `DeclineStatic`. No declaration/field/member/factory loops, rebinding decisions, constructor IL or canonical admission remain in them. `TryEmitMemberIterator` (2794–2862) stays separate enclosing-member discovery/admission, supplying the owner at2810/2858; top-level calls are4168/4170. No hidden C# callback or widened async/generic instance admission.

**Consumed identity.** The unchanged planner has exactly **15 raw AddType calls**: nine state-machine entries and bool, Exception, ValueTask, conditional boxed element, captured factory-field type and TaskAwaiter. Pass the existing catalog table through `ColumnarIteratorEmitContext` (1202/1225) and its already-retained recursive MoveNext context. Select exact runtime companions at each old AddType evaluation; no eager selection, fresh-table fallback or signature matrix. If no new calls appear, the projection is6→21 keyed and30→15 legacy out of36. Method/field pool identities are outside this claim.

Machine/synthesized VAR registration already occurs at2545/2708. Factory method MVAR registration is now `ForSourceMethod` call4009, registry1225. Generic members use machine self-instantiation; factories separately rebind over method MVARs. Before broad edits, execute a real generator with T and T[] captures and assert exact self/factory/field key companions and runtime enumeration. Consume actual rebound `FieldInfo.FieldType` at factory1775, never a reconstructed signature hiding VAR/MVAR differences. An invalid/unregistered exact companion is a named prerequisite. Preserve the existing sync generic factory elementType and omitted enclosing/known extras.

**Order.** Sync defines/registers before element resolution; async resolves/admit before defining/registering. Preserve sync hoisted-enumerator special resolution, direct machine-map priority, false/out values and recursive MVAR checks. Replay both raw constructors in N# at their existing phase, including async continuation/core handles; typed constructor CodePlan conversion is not claimed. Preserve all11 Apply→BuildPlan→GetILGenerator→Execute phases, async core-before-interface order, and factory completion before synthesizedTypes.Add. Return decline facts at the old failure points; the wrapper records unchanged site/message/member/span under the existing source-file scope. No callback or copied diagnostic storage owner.

**Acceptance and stop condition.** Reuse native sync/generic/async suites and the precomputed-shape ordering seam, positive twins and earlier body/attachment progress on later failures. Add actual15-site keyed/table assertions plus fresh-plan companion corruption before IL/local creation; never reuse a consumed positive. Cover self/factory owners, generic Current boxing, hoisted disposal, enclosing this, async completion/exception/await and decline timing. First measure exact rebinding, raw constructor Emit and table selection under the live seed. Any failure needs a bounded prerequisite before this full cut; no partial driver/raw-row fallback/admission widening qualifies. **Execution remains contingent on i acceptance.**
