---
sidebar_label: NL321
title: "NL321: a sized array allocation cannot take constructor arguments"
---

# NL321: a sized array allocation cannot take constructor arguments

`new T[n]` is an array allocation, not a constructor call. It takes a length and nothing else — the
elements come back zero-initialized, and there is no constructor to pass arguments to. Writing both
a size and an argument list is NL321.

```n#
func Make(): int[] {
    return new int[4](7)          // ERROR NL321
}
```

The underline is on `new`, because the mistake is which *form* of `new` this is, not which argument
is wrong.

```text
── [NL321] ERROR ──────────────────────────── Program.nl:2:12 ──

    2 |     return new int[4](7)          // ERROR NL321
      |            ^^^

Sized array allocation cannot also pass constructor arguments

Suggestion: Use 'new T[n]' for a zero-initialized array, or write the elements as a list — 'values := [1, 2, 3]'.
```

## Why is this an error?

`new int[4](7)` reads as though it might mean "four sevens", and it cannot: `new T[n]` maps directly
onto the CLR's array allocation, which has one input — the length. There is no per-element
initializer in that instruction and no constructor on `int[]` to receive `7`.

N# could have picked one of the two plausible readings and ignored the rest of the syntax. It reports
instead, because the two readings produce different arrays and nothing in the line says which one you
meant.

## How to fix it

If you wanted **`n` elements to fill in later**, drop the arguments — the array is already
zero-initialized:

```n#
func Zeros(n: int): int[] {
    return new int[n]
}
```

If you wanted **specific elements**, write them as a list:

```n#
func Values(): int[] {
    values := [1, 2, 3]
    return values
}
```

And if you wanted a fixed size with known contents, allocate and assign:

```n#
func Values(): int[] {
    values := new int[3]
    values[0] = 1
    values[1] = 2
    values[2] = 3
    return values
}
```

## What NL321 is not about

**Ordinary constructor calls are untouched.** `new Point(1, 2)` is a constructor call with
arguments, and this rule has nothing to say about it — NL321 fires only when a `[...]` size is
present *and* an argument list is present on the same `new`.

**A wrong number of constructor arguments is [NL401](./NL401.md)**, and a wrong argument *type* is [NL202](./NL202.md).
NL321 is not a count or a type complaint; it is the two syntaxes having been combined.

## A note on `new T[] { ... }`

That spelling parses, and the backend does not currently lower it: a function containing one stops
at [NL103](./NL103.md) with `parse.function`. NL321 used to suggest it, which cost the reader the
edit and then blamed them for taking it — the diagnostic now suggests the bracket literal
`[1, 2, 3]` shown above, which compiles today, and `tests/native/diagnostic-honesty` runs the
suggested text through the shipped compiler on every gate so it cannot drift back.

## Related

- [NL103](./NL103.md) — a construct the code generator cannot yet lower.
- [NL401](./NL401.md) — the wrong number of arguments to a call that does take arguments.
