# Systems Proof Project Audit

Date: 2026-06-02
Status: mixed executable proof report and compiler audit

The projects under `docs/design/systems-samples/proofs/` are design proof inputs
for use cases 24-48 in `docs/design/systems-nsharp.md`. Only projects marked
`executable` below are current compiler evidence, and only for the listed gates.
Projects marked `design-only` must not be cited as passing implementation
evidence until migrated and verified by `nlc check --systems-report`,
`nlc build --perf-report`, and any required interop/AOT gate.

Current audit command shape:

```bash
dotnet src/NSharpLang.Cli/bin/Debug/net10.0/Cli.dll check --project <proof-dir> --systems-report
```

Current status summary from the 2026-06-02 audit:

| Project | Status | Check evidence | Build/perf evidence | Other required evidence | Remaining gap |
| --- | --- | --- | --- | --- | --- |
| `24-zero-copy-frame-reader` | executable | passes, 0 errors, 0 warnings | passes `nlc build --perf-report`; no allocation, boxing, dispatch, trap, hot-readiness, or AOT blockers | emitted assembly runs with copied runtime asset; hot `NextFrame` returns a local ref-struct `FrameResult` carrying the zero-copy `ReadOnlySpan<byte>` and reports `BinaryPrimitives.ReadInt32LittleEndian` plus `reader.buf.Slice`; lifetime-only `'a` is erased from the CLR signature | Runtime `Result<T,E>` is intentionally not used with ref-like payloads in this proof; broader ref-like result ABI remains future runtime work. |
| `25-trusted-memory-copy` | executable | passes, 0 errors, 0 warnings | passes `nlc build --perf-report`; no allocation, boxing, dispatch, trap, hot-readiness, or AOT blockers; reports trusted site | emitted assembly runs with copied runtime asset; passes `nlc query trusted`; hot `CopyExact` uses governed `Buffer.MemoryCopy` through `Span<T>.ptr`/`ReadOnlySpan<T>.ptr` | Broader pointer syntax remains intentionally narrow; v1 only supports span `.ptr` for trusted memory-copy wrappers. |
| `26-native-device-handle` | executable | passes, 0 errors, 1 warning | passes `nlc build --perf-report`; no allocation, dispatch, trap, or AOT blockers | native `LibraryImport` declarations emit no managed body; direct IL verification passes | Portable execution is not claimed because native library resolution is platform/deployment-specific. |
| `27-c-library-cli` | executable | passes, 0 errors, 1 warning | passes `nlc build --perf-report`; no allocation, trap, dispatch, or AOT blockers | native `LibraryImport` declaration emits no managed body; direct IL verification passes | Real native library execution and NativeAOT publish remain external deployment proofs. |
| `28-nativeaot-json-cli` | design-only | 7 errors, 3 warnings | missing | native image deferred | NativeAOT publish proof is analysis-only; sample syntax is not current. |
| `29-generated-regex-boundary` | design-only | 23 errors, 2 warnings | missing | missing | Generated regex boundary syntax and summaries are not executable. |
| `30-cold-failure-logging` | executable | passes, 0 errors, 2 warnings | passes `nlc build --perf-report`; reports one boundary allocation in `LogColdFailure` and no delegate, boxing, dispatch, trap, hot-readiness, or AOT blockers | emitted assembly runs with copied runtime asset; direct IL verification passes; hot `ParseMagic` stays allocation-free while boundary `ParseWithColdLogging` logs only after an error | The logger uses a generic message because interpolated-string holes are not currently counted as parameter reads by unused-parameter diagnostics; richer diagnostic-detail logging remains follow-up polish. |
| `31-hot-metrics` | executable | passes, 0 errors, 1 warning | passes `nlc build --perf-report`; no allocation, trap, or AOT blockers | hot metrics proof covers `Interlocked.Increment` on byref struct fields | Console output remains a known non-hot warning in `Main`. |
| `32-cache-prewarm` | executable | passes, 0 errors, 1 warning | passes `nlc build --perf-report`; no hot-readiness, trap, or AOT blockers | warmup config and static table initialization are compiler-covered | Console output remains a known non-hot warning in `Main`. |
| `33-arraypool-file-io` | executable | passes, 0 errors, 2 warnings | passes `nlc build --perf-report`; no allocation, pool, resource, dispatch, trap, hot-readiness, or AOT blockers | emitted assembly runs with copied runtime asset; direct IL verification passes; boundary `ReadAndParse` rents an `ArrayPool<byte>` buffer, reads a copied runtime DLL through `File.OpenRead`, parses via hot `ReadOnlySpan<byte>`, disposes the stream, and returns the buffer on the lexical path | File IO remains a boundary-review warning, and exceptional-control-flow/finally pool return remains future ownership work. |
| `34-memorypool-disposal` | executable | passes, 0 errors, 0 warnings | passes `nlc build --perf-report`; no allocation, pool, resource, dispatch, trap, hot-readiness, or AOT blockers | emitted assembly runs with copied runtime asset; direct IL verification passes; hot `FillHeader` writes caller-owned span while boundary `BuildMessage` rents an `IMemoryOwner<byte>`, uses it, and disposes it on the lexical path | This proof covers explicit dispose balance for `MemoryPool<T>` owners; broader exceptional control-flow/finally balance remains future ownership work. |
| `35-async-file-hot-parser` | executable | passes, 0 errors, 4 warnings | passes `nlc build --perf-report`; reports two async boundary leak sites and no allocation, delegate, boxing, dispatch, trap, hot-readiness, or AOT blockers | emitted assembly runs with copied runtime asset | Executable proof covers async boundary IO feeding a hot parser with a primitive `Task<int>` handoff; `ValueTask<Result<T,E>>` remains a compiler/analyzer gap. |
| `36-dictionary-setup-hot-read` | executable | passes, 0 errors, 3 warnings | passes `nlc build --perf-report`; reports setup allocation/boundary leak and no hot-readiness/trap/AOT blockers | hot `Dictionary.TryGetValue` read is summary-covered only for registered `Dictionary` members | Boundary return shape intentionally remains a warning. |
| `37-fixed-capacity-map` | executable | passes, 0 errors, 1 warning | passes `nlc build --perf-report`; reports one boundary allocation in `NewMap` and no boxing, dispatch, boundary leak, trap, hot-readiness, or AOT blockers | emitted assembly runs with copied runtime asset; hot `Put`/`Get` operate over preallocated fixed storage and exercise `Result<int, MapError>` over a generated enum | Construction remains a reviewed boundary allocation; broader deletion, probing strategy, and resize policies are outside this proof. |
| `38-unmanaged-sort-comparer` | executable | passes, 0 errors, 1 warning | passes `nlc build --perf-report`; reports one boundary allocation in `Main` and no delegate, boxing, dispatch, boundary leak, trap, hot-readiness, or AOT blockers | emitted assembly runs with copied runtime asset; proof test decodes hot `SortPair` IL and requires `constrained.` with no `box` opcode | Uses a current N# `struct` constrained sortable contract; the exact `unmanaged` keyword and separate `TComparer : ValueComparer<T>` inference remain compiler polish. |
| `39-hot-linq-pipeline` | executable | passes, 0 errors, 1 warning | passes `nlc build --perf-report`; reports one boundary allocation in `Main` and no delegate, closure, boxing, dispatch, boundary leak, trap, hot-readiness, or AOT blockers | emitted assembly runs with copied runtime asset; hot extension-method pipeline over `ReadOnlySpan<int>` compiles without LINQ delegate/closure shape | Uses a current N# extension-method contract for hot-compatible pipeline evidence; it does not claim direct ZLinq/package execution. |
| `40-csharp-hot-parser-api` | executable | passes, 0 errors, 0 warnings | passes `nlc build --perf-report`; no allocation, dispatch, trap, or AOT blockers | C# `ProjectReference` consumer compiles and runs `PacketApi.ParseHeader`, preserving `ReadOnlySpan<byte>` and `Result<Header, HeaderError>` ABI through mismatched `.csproj`/`project.yml` names | Broader C# ABI matrix and native publish remain future deployment proofs. |
| `41-structured-errors` | executable | passes, 0 errors, 0 warnings | passes `nlc build --perf-report`; no allocation, boxing, dispatch, trap, hot-readiness, or AOT blockers | emitted assembly runs with copied runtime asset; hot `ReadTaggedByte` returns structured `ParseError` through `Result<byte, ParseError>` and validates short, bad-tag, and ok paths | Boundary exception translation remains a separate design proof; this row covers allocation-free structured error values. |
| `42-aot-friendly-public-api` | executable | passes, 0 errors, 2 warnings | passes `nlc build --perf-report`; no allocation, delegate, boxing, dispatch, trap, hot-readiness, or AOT blockers | `nlc check --systems-report` reports `aot.analysis=pass`, `trimSafe=true`, and `NameApi.Normalize` as `aotSafe` for the NativeAOT target; direct IL verification passes | Native image emission remains deferred; trim-safety is currently report-level project analysis rather than a source attribute. |
| `43-mono-wasm-plugin` | executable | passes, 0 errors, 0 warnings | passes `nlc build --perf-report`; no allocation, dispatch, trap, or AOT blockers | target-qualified `aotSafe(mono-wasm)` analysis compiles | Actual Mono/WASM runner remains an external deployment proof. |
| `44-ci-allocation-gate` | executable | passes, 0 errors, 2 warnings | passes `nlc build --perf-report`; reports boundary allocation and no AOT blockers | fast Systems BenchmarkDotNet gate covers allocation/perf CI enforcement; detailed matrix remains explicit deep mode | No native image proof required for this use case. |
| `45-trusted-audit` | executable | passes, 0 errors, 0 warnings | passes `nlc build --perf-report`; reports trusted site | passes `nlc query trusted` with owner/review/expiry/unsafe metadata | Broader unsafe-wrapper projects still pending. |
| `46-dapper-boundary` | executable | passes, 0 errors, 2 warnings | passes `nlc build --perf-report`; reports two boundary allocation sites and no delegate, boxing, dispatch, boundary leak, trap, hot-readiness, or AOT blockers | emitted assembly runs with copied runtime asset | Direct proof uses a self-contained database-adapter boundary; real Dapper/EF NuGet execution remains external package interop work. |
| `47-cli-startup-honesty` | design-only | 12 errors, 4 warnings | missing | native image deferred | Startup/AOT/readiness proof is broader than current implementation. |
| `48-effect-drift` | executable | passes, 0 errors, 1 warning | passes `nlc build --perf-report`; reports boundary allocation and no hot-readiness/trap/AOT blockers | source-inferred helper allocation drift is covered by an adversarial systems test | Broad external package identity drift proof remains deferred to sidecar/package coverage. |

Executable Systems N# evidence currently lives in
`tests/fixtures/systems-gauntlet/`, the executable proof projects listed above,
`tests/SystemsNSharpTests.cs`, the fast BenchmarkDotNet gate, and the detailed
BenchmarkDotNet matrix mode in `benchmarks/Systems*Benchmarks.cs` enforced by
`scripts/benchmark-systems.sh`.
Those are the artifacts that must be cited for current implementation status.
